import { Button } from '@chakra-ui/react';
import { AddressType } from '../../types';

const UpdateAddress = ({}: AddressType) => {
  return (
    <Button colorScheme='teal' variant='outline'>
      Modifier
    </Button>
  );
};

export default UpdateAddress;
